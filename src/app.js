/* src/app.js */
import $ from 'jquery';
// Styles
import 'styles/_app.scss';

$(document).ready(() => {
  require('scripts/jssocials.min');
  require('scripts/thegrid');
  require('scripts/demo');
  require('scripts/animations');
  require('scripts/map');
  require('scripts/carousel');
  require('scripts/script');
});
